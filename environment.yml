name: mcp_router
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - pip:
    - mcp>=1.0.0
    - fastapi>=0.104.0
    - uvicorn>=0.24.0
    - httpx>=0.25.0
    - pydantic>=2.0.0
    - pydantic-settings>=2.0.0
    - watchdog>=3.0.0
    - python-dotenv>=1.0.0
    - pytest>=7.0.0
    - pytest-asyncio>=0.21.0

